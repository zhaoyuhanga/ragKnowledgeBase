# Phase 4：运营体系

**版本：** V1.0  
**日期：** 2026-05-17  
**预计周期：** 3-4 周  
**优先级：** P1

---

## 一、阶段概述

### 1.1 阶段目标

构建数据驱动的运营能力，包括监控告警、配额管理和运营数据看板，实现精细化运营。

### 1.2 核心功能

| 模块 | 功能点 | 优先级 | 工作量 |
|------|--------|--------|--------|
| 监控告警 | 系统监控/告警规则/通知 | P1 | 5 天 |
| 配额管理 | 用户配额/套餐管理/超限处理 | P1 | 3 天 |
| 运营数据 | 数据看板/趋势分析/报表 | P2 | 4 天 |

### 1.3 技术架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Phase 4 架构                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                       监控指标采集层                                    │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │ │
│  │  │ 系统指标 │ │ 应用指标 │ │ 业务指标 │ │ 成本指标 │           │ │
│  │  │(Prometheus)│ │(APM)  │ │(业务埋点)│ │(计费系统)│           │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                       数据存储层                                       │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │ │
│  │  │Prometheus│ │  Grafana │ │  ClickHouse│ │ Redis   │           │ │
│  │  │ 时序数据  │ │  可视化   │ │  OLAP    │ │  热数据  │           │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                       告警处理层                                       │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐                          │ │
│  │  │ 告警规则 │→│ 告警评估 │→│ 告警通知 │                          │ │
│  │  └──────────┘ └──────────┘ └──────────┘                          │ │
│  │                                    │                                │ │
│  │  通知渠道: 邮件/短信/钉钉/飞书/企业微信/Webhook                    │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、功能需求详述

### 2.1 监控告警系统

#### 2.1.1 监控指标体系

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           监控指标体系                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                        系统层指标                                      │ │
│  │  ─────────────────────────────────────────────────────────────────│ │
│  │  CPU: 使用率、负载、上下文切换                                       │ │
│  │  内存: 使用率、Swap、缓存命中率                                      │ │
│  │  磁盘: 使用率、IOPS、吞吐量                                         │ │
│  │  网络: 带宽使用、连接数、丢包率                                      │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                        应用层指标                                      │ │
│  │  ─────────────────────────────────────────────────────────────────│ │
│  │  请求: QPS、延迟(P50/P95/P99)、错误率                              │ │
│  │  队列: 积压深度、消费速率                                            │ │
│  │  连接: 数据库连接池、Redis 连接池                                    │ │
│  │  缓存: 命中率、内存使用                                              │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                        业务层指标                                      │ │
│  │  ─────────────────────────────────────────────────────────────────│ │
│  │  用户: DAU、MAU、新增用户、留存率                                    │ │
│  │  知识库: 创建数、文档数、向量数                                      │ │
│  │  问答: 问答量、成功率、满意度                                        │ │
│  │  Token: 消耗量、峰值                                                │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                        成本层指标                                      │ │
│  │  ─────────────────────────────────────────────────────────────────│ │
│  │  API成本: 调用次数 × 单价                                            │ │
│  │  存储成本: 向量存储、文件存储                                         │ │
│  │  计算成本: Embedding 计算、LLM 调用                                  │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 2.1.2 告警规则配置

```yaml
alert_rules:
  # 系统告警
  - name: "high_cpu_usage"
    condition: "cpu_usage > 80%"
    duration: "5m"
    severity: "warning"
    notification:
      channels: ["email", "slack"]
      
  - name: "critical_cpu_usage"
    condition: "cpu_usage > 95%"
    duration: "1m"
    severity: "critical"
    notification:
      channels: ["sms", "phone", "slack"]
      
  # 应用告警
  - name: "high_error_rate"
    condition: "error_rate > 5%"
    duration: "5m"
    severity: "warning"
    notification:
      channels: ["email", "slack"]
      
  - name: "high_latency"
    condition: "p95_latency > 2000ms"
    duration: "5m"
    severity: "warning"
    notification:
      channels: ["slack"]
      
  # 业务告警
  - name: "quota_exceeded"
    condition: "user.quota_usage > 90%"
    duration: "1m"
    severity: "warning"
    notification:
      channels: ["in_app"]
      
  - name: "low_satisfaction"
    condition: "satisfaction < 80%"
    duration: "1h"
    severity: "warning"
    notification:
      channels: ["email"]
      
  # 成本告警
  - name: "high_api_cost"
    condition: "daily_cost > threshold"
    duration: "1d"
    severity: "warning"
    notification:
      channels: ["email"]
```

#### 2.1.3 需求规格

| 需求ID | 需求描述 | 验收条件 | 验证方法 |
|--------|----------|----------|----------|
| MON-001 | 指标采集 | 采集系统/应用/业务指标 | 指标数据正确 |
| MON-002 | 指标展示 | 仪表盘展示关键指标 | 展示正确 |
| MON-003 | 历史趋势 | 查看指标历史趋势 | 趋势正确 |
| MON-004 | 告警规则 | 配置告警规则 | 规则生效 |
| MON-005 | 告警触发 | 条件满足触发告警 | 告警触发 |
| MON-006 | 告警通知 | 多种渠道发送通知 | 通知送达 |
| MON-007 | 告警抑制 | 抑制重复告警 | 抑制生效 |
| MON-008 | 告警恢复 | 恢复后发送恢复通知 | 恢复通知 |

#### 2.1.4 接口设计

```
# 监控指标
GET    /api/v1/monitoring/metrics/current       # 当前指标
GET    /api/v1/monitoring/metrics/history      # 历史指标
GET    /api/v1/monitoring/dashboards           # 仪表盘
GET    /api/v1/monitoring/dashboards/{id}      # 仪表盘详情

# 告警规则
GET    /api/v1/alerts/rules                    # 告警规则列表
POST   /api/v1/alerts/rules                    # 创建规则
GET    /api/v1/alerts/rules/{id}              # 规则详情
PUT    /api/v1/alerts/rules/{id}              # 更新规则
DELETE /api/v1/alerts/rules/{id}              # 删除规则
POST   /api/v1/alerts/rules/{id}/test         # 测试规则

# 告警事件
GET    /api/v1/alerts/events                  # 告警事件列表
GET    /api/v1/alerts/events/{id}            # 事件详情
POST   /api/v1/alerts/events/{id}/acknowledge # 确认告警
POST   /api/v1/alerts/events/{id}/resolve     # 解决告警

# 通知渠道
GET    /api/v1/notifications/channels         # 渠道列表
POST   /api/v1/notifications/channels          # 创建渠道
PUT    /api/v1/notifications/channels/{id}    # 更新渠道
DELETE /api/v1/notifications/channels/{id}    # 删除渠道
POST   /api/v1/notifications/channels/{id}/test # 测试渠道
```

### 2.2 配额管理系统

#### 2.2.1 配额套餐设计

```yaml
# 配额套餐配置
quota_tiers:
  free:
    display_name: "免费版"
    price: 0
    
    # 文档配额
    documents:
      total: 50          # 文档总数上限
      per_month: 10      # 每月新增上限
      storage_mb: 100    # 存储上限(MB)
      
    # API配额
    api:
      calls_per_day: 100
      calls_per_month: 1000
      rate_limit: 10      # 次/分钟
      
    # Token配额
    tokens:
      per_month: 50000   # 月Token上限
      
    # 知识库配额
    knowledge_bases:
      count: 1           # 知识库数量
      users: 1           # 成员数量
      
    # 功能限制
    features:
      sso: false
      audit_log: false
      api_access: false
      priority_support: false
      
  pro:
    display_name: "专业版"
    price: 299
    
    documents:
      total: 5000
      per_month: 500
      storage_mb: 5120
      
    api:
      calls_per_day: 10000
      calls_per_month: 100000
      rate_limit: 100
      
    tokens:
      per_month: 5000000
      
    knowledge_bases:
      count: 10
      users: 50
      
    features:
      sso: true
      audit_log: true
      api_access: true
      priority_support: true
      
  enterprise:
    display_name: "企业版"
    price: 定制
    
    # 无限制，可定制
    documents:
      total: -1  # 无限制
      per_month: -1
      storage_mb: -1
      
    api:
      calls_per_day: -1
      rate_limit: 1000
      
    tokens:
      per_month: -1
      
    knowledge_bases:
      count: -1
      users: -1
      
    features:
      sso: true
      audit_log: true
      api_access: true
      priority_support: true
      dedicated_support: true
      custom_integration: true
```

#### 2.2.2 配额检查流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           配额检查流程                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  用户发起请求                                                              │
│       │                                                                   │
│       ▼                                                                   │
│  ┌─────────────────┐                                                     │
│  │ 检查用户配额套餐 │                                                     │
│  └────────┬────────┘                                                     │
│           │                                                               │
│     ┌─────┴─────┐                                                        │
│     ▼           ▼                                                        │
│  免费用户     付费用户                                                     │
│     │           │                                                        │
│     ▼           ▼                                                        │
│  检查免费配额  检查套餐配额                                                │
│     │           │                                                        │
│     └─────┬─────┘                                                        │
│           │                                                               │
│           ▼                                                               │
│  ┌─────────────────┐                                                     │
│  │ 配额充足?       │                                                     │
│  └────────┬────────┘                                                     │
│           │                                                               │
│     ┌─────┴─────┐                                                        │
│     ▼           ▼                                                        │
│   充足        不足                                                        │
│     │           │                                                        │
│     ▼           ▼                                                        │
│  执行请求    返回配额不足                                                  │
│     │           │                                                        │
│     ▼           │                                                        │
│  扣减配额    引导升级                                                    │
│     │                                                               │
│     ▼                                                                   │
│  返回结果                                                              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 2.2.3 需求规格

| 需求ID | 需求描述 | 验收条件 | 验证方法 |
|--------|----------|----------|----------|
| QUO-001 | 套餐展示 | 展示所有套餐信息 | 显示正确 |
| QUO-002 | 套餐切换 | 支持套餐升级/降级 | 切换成功 |
| QUO-003 | 配额查询 | 查询用户当前配额 | 数据正确 |
| QUO-004 | 配额扣减 | 执行请求时扣减配额 | 扣减正确 |
| QUO-005 | 配额超限 | 超限时阻止请求 | 阻止生效 |
| QUO-006 | 配额重置 | 按周期重置配额 | 重置正确 |
| QUO-007 | 配额预警 | 接近限额时预警 | 预警触发 |
| QUO-008 | 配额统计 | 统计配额使用情况 | 统计正确 |

#### 2.2.4 接口设计

```
# 套餐管理
GET    /api/v1/quotas/tiers               # 套餐列表
GET    /api/v1/quotas/tiers/{tier_id}     # 套餐详情

# 用户配额
GET    /api/v1/quotas/me                 # 我的配额
GET    /api/v1/quotas/me/usage          # 我的使用量
GET    /api/v1/quotas/me/history         # 使用历史

# 管理员配额管理
GET    /api/v1/admin/quotas/users/{user_id}  # 用户配额
PUT    /api/v1/admin/quotas/users/{user_id}  # 修改用户配额
POST   /api/v1/admin/quotas/users/{user_id}/reset # 重置配额

# 配额检查
POST   /api/v1/quotas/check              # 检查配额
  Body: {
    type: "document" | "api_call" | "token",
    amount: number
  }
  Response: {
    allowed: boolean,
    current: number,
    limit: number,
    remaining: number
  }
```

### 2.3 运营数据看板

#### 2.3.1 看板设计

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           运营数据看板                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                        总览看板                                       │ │
│  │  ─────────────────────────────────────────────────────────────────│ │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐                 │ │
│  │  │  DAU   │ │  问答量 │ │  成本   │ │ 满意度  │                 │ │
│  │  │ 1,234  │ │ 5,678  │ │ ¥12,345 │ │  92%   │                 │ │
│  │  │  ↑12%  │ │  ↑8%   │ │  ↓5%    │ │  ↑2%   │                 │ │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘                 │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                        趋势图表                                       │ │
│  │  ─────────────────────────────────────────────────────────────────│ │
│  │       [用户趋势图]          [问答趋势图]         [成本趋势图]         │ │
│  │       ~~~~~~              ~~~~~~~            ~~~~~~                │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                        排行榜                                        │ │
│  │  ─────────────────────────────────────────────────────────────────│ │
│  │  热门知识库 Top10    活跃用户 Top10     高频问题 Top10              │ │
│  │  1. 产品文档        @user1            Q1: 如何使用?                │ │
│  │  2. 技术文档        @user2            Q2: 功能介绍?                │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 2.3.2 数据指标定义

| 指标类型 | 指标名称 | 计算方式 | 更新频率 |
|----------|----------|----------|----------|
| **用户指标** | DAU | 当日活跃用户数 | 实时 |
| | MAU | 当月活跃用户数 | 每日 |
| | 新增用户 | 当日新增注册 | 实时 |
| | 次日留存 | 次日回访用户/新增用户 | 每日 |
| **知识库指标** | 总数 | 知识库总数 | 实时 |
| | 文档总数 | 所有知识库文档数 | 实时 |
| | 向量总数 | 所有向量数 | 实时 |
| **问答指标** | 日问答量 | 当日问答次数 | 实时 |
| | 成功率 | 成功问答/总问答 | 实时 |
| | 平均延迟 | 平均响应时间 | 实时 |
| | 满意度 | 好评/总评价 | 每日 |
| **成本指标** | API成本 | 调用量 × 单价 | 每日 |
| | 存储成本 | 存储量 × 单价 | 每日 |
| | Token成本 | Token消耗 × 单价 | 每日 |

#### 2.3.3 需求规格

| 需求ID | 需求描述 | 验收条件 | 验证方法 |
|--------|----------|----------|----------|
| DSH-001 | 总览看板 | 展示核心指标 | 展示正确 |
| DSH-002 | 趋势图表 | 展示指标趋势 | 图表正确 |
| DSH-003 | 排行榜 | 展示各类排名 | 排名正确 |
| DSH-004 | 时间筛选 | 支持时间范围筛选 | 筛选正确 |
| DSH-005 | 对比分析 | 支持同比/环比 | 分析正确 |
| DSH-006 | 导出报表 | 导出数据报表 | 导出正确 |
| DSH-007 | 订阅报表 | 定时发送报表 | 订阅生效 |
| DSH-008 | 自定义看板 | 自定义看板布局 | 配置生效 |

#### 2.3.4 接口设计

```
# 统计概览
GET    /api/v1/stats/overview              # 总览统计
GET    /api/v1/stats/trend                # 趋势统计
GET    /api/v1/stats/rankings             # 排行榜

# 用户统计
GET    /api/v1/stats/users/dau            # 日活
GET    /api/v1/stats/users/mau            # 月活
GET    /api/v1/stats/users/retention      # 留存
GET    /api/v1/stats/users/new            # 新增

# 知识库统计
GET    /api/v1/stats/knowledge-bases      # 知识库统计
GET    /api/v1/stats/documents            # 文档统计
GET    /api/v1/stats/vectors              # 向量统计

# 问答统计
GET    /api/v1/stats/qa/daily             # 日统计
GET    /api/v1/stats/qa/hourly            # 小时统计
GET    /api/v1/stats/qa/satisfaction      # 满意度
GET    /api/v1/stats/qa/questions         # 问题分析

# 成本统计
GET    /api/v1/stats/costs/daily          # 日成本
GET    /api/v1/stats/costs/monthly        # 月成本
GET    /api/v1/stats/costs/breakdown      # 成本分解
GET    /api/v1/stats/costs/forecast       # 成本预测

# 报表
POST   /api/v1/reports                    # 生成报表
GET    /api/v1/reports/{id}             # 报表详情
GET    /api/v1/reports/{id}/download     # 下载报表
GET    /api/v1/reports/templates         # 报表模板
```

---

## 三、非功能需求

### 3.1 性能需求

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 监控数据延迟 | ≤ 30s | 指标采集到展示 |
| 告警触发延迟 | ≤ 1min | 条件触发到告警 |
| 看榜查询延迟 | ≤ 2s | 数据查询响应 |
| 报表生成 | ≤ 30s | 小型报表生成 |

### 3.2 可用性需求

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 监控服务可用性 | ≥ 99.9% | SLA 保障 |
| 数据准确性 | ≥ 99.5% | 数据正确性 |
| 告警准确率 | ≥ 95% | 误报率 < 5% |

---

## 四、验收清单

### 4.1 监控告警验收

| 序号 | 验收项 | 验收条件 | 验证方法 | 状态 |
|------|--------|----------|----------|------|
| 1 | 指标采集 | 系统/应用/业务指标采集 | 数据验证 | ⬜ |
| 2 | 仪表盘 | 关键指标仪表盘展示 | 功能测试 | ⬜ |
| 3 | 历史趋势 | 查看历史趋势图表 | 功能测试 | ⬜ |
| 4 | 告警规则 | 创建和管理告警规则 | 功能测试 | ⬜ |
| 5 | 告警触发 | 条件满足触发告警 | 模拟测试 | ⬜ |
| 6 | 告警通知 | 多种渠道发送通知 | 功能测试 | ⬜ |
| 7 | 告警抑制 | 抑制重复告警 | 功能测试 | ⬜ |
| 8 | 告警恢复 | 恢复发送通知 | 功能测试 | ⬜ |

### 4.2 配额管理验收

| 序号 | 验收项 | 验收条件 | 验证方法 | 状态 |
|------|--------|----------|----------|------|
| 1 | 套餐展示 | 展示所有套餐信息 | 功能测试 | ⬜ |
| 2 | 套餐切换 | 升级/降级套餐 | 功能测试 | ⬜ |
| 3 | 配额查询 | 查询当前配额使用 | 功能测试 | ⬜ |
| 4 | 配额扣减 | 执行时扣减配额 | 功能测试 | ⬜ |
| 5 | 配额限制 | 超限阻止请求 | 功能测试 | ⬜ |
| 6 | 配额重置 | 按周期自动重置 | 等待周期验证 | ⬜ |
| 7 | 配额预警 | 接近限额预警 | 功能测试 | ⬜ |
| 8 | 配额统计 | 统计使用情况 | 功能测试 | ⬜ |

### 4.3 运营看板验收

| 序号 | 验收项 | 验收条件 | 验证方法 | 状态 |
|------|--------|----------|----------|------|
| 1 | 总览看板 | 展示核心指标 | 功能测试 | ⬜ |
| 2 | 趋势图表 | 展示趋势变化 | 功能测试 | ⬜ |
| 3 | 排行榜 | 展示各类排名 | 功能测试 | ⬜ |
| 4 | 时间筛选 | 时间范围筛选 | 功能测试 | ⬜ |
| 5 | 对比分析 | 同比/环比分析 | 功能测试 | ⬜ |
| 6 | 导出报表 | 导出数据报表 | 功能测试 | ⬜ |
| 7 | 订阅报表 | 定时发送报表 | 功能测试 | ⬜ |
| 8 | 自定义看板 | 自定义布局 | 功能测试 | ⬜ |

---

## 五、风险与应对

| 风险 | 影响 | 可能性 | 应对措施 |
|------|------|--------|----------|
| 监控数据量大 | 高 | 中 | 数据采样/降采样 |
| 告警误报率高 | 中 | 中 | 规则优化/智能抑制 |
| 报表生成慢 | 低 | 中 | 预计算/缓存 |

---

## 六、版本历史

| 版本 | 日期 | 修改内容 | 作者 |
|------|------|----------|------|
| V1.0 | 2026-05-17 | 初始版本 | - |
