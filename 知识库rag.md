导入 → 切分 → 存储 → 检索 （要求混合检索  向量检索+关键词检索）

1，解析文件，word有标记文档直接解析，比如pdf和图片无标记文档缺少结构信息，先文档分割，将pdf文件按单页拆分，清晰度识别区分电子版和扫描版，扫描版重点关注orc识别效果 ，元素切割文字图表图片等内容先用ocr做结构切割拆分不同模块分别识别，图片和表格用专用ocr处理提升识别精度，版面分析存在单页双行跨页等多种版面结构，ocr识别之后零散文字需要将文字还原为原文结构。

2，切片 不同文档结构对应不同切片策略，语义切片借助模型预测句子间语义连贯性，切出语义完整的文本块，设置重叠度合理配置分块重叠比例为信息完整兜底，特殊场景处理若图表为重要内容将图表标题和对应内容合并切片保证图表信息完整

3，检索 查询重写（口语化问题改的更清晰）多查询生成（一个问题拆成3到5个相似问题 比如用户提问如何提高RAG性能 拆分成 RAG性能优化方案，RAG检索精准技巧，RAG性能瓶颈解决方案）  子查询分解（多层含义的复杂问题拆成多个子查询分别检索在整合答案）假设文档嵌入（先让大模型生成假设答案，在用假设答案去文档检索）后退提示（具体的问题改成宏观通用问题，先检索背景信息再回答原问题 比如用户提问我查询产品价格，但知识库无法直接答案，就先查公司的定价策略同类价格体系在基于背景回复）先规范化重新，在做多查询扩覆盖最后用重排序模型优化结果，可以根据业务场景灵活调整可持续优化



4，文档版本，清洗数据，清洗规则数据库配置

			文件上传 → 格式识别 → 哈希去重 → 批量管理



&#x20;                                 ↓



&#x20;                       编码修复 → 乱码检测 → 噪声过滤 → 正文提取 → PDF解析要结构感知



&#x20;                                 ↓                    ↓



&#x20;                       广告过滤 → 脚注清理 → 质量评估 → 决定是否入库



&#x20;                                 ↓



&#x20;                       关键词匹配 → 结构分析 → 置信度评估 → 分类结果



&#x20;                                 ↓



&#x20;                       格式检测 → Markdown转换 → 优化处理

